using System;

class Trailing0InN
{
    static void Main()
    {
        int n = int.Parse(Console.ReadLine());

        for (int i = 1; i <= n.ToString().Length; i++)
        {
            if (n.ToString().EndsWith("0"))
            {

            }
        }
    }
}
